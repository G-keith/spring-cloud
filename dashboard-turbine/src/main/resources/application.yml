server:
  port: 9004
spring:
  application:
    name: dashboard-turbine
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9001/eureka/
turbine:
  app-config: cloud-hystrix-dashboard
  aggregator:
    cluster-config: default
  cluster-name-expression: new String("default")
